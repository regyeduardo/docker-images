"""
``services.generate`` — módulo de geração de conteúdo via DeepSeek (OpenAI-compatible).

Funções exportadas:
- ``gerar_markdown`` — envia conteúdo multimodal para a API e retorna Markdown
- ``montar_conteudo_multimodal`` — constrói o payload de content parts
- ``extrair_texto_documento`` — extrai texto de PDF, DOCX ou TXT
"""
